{
  "name": "codex-orgst",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo raiz - git hooks e configs compartilhadas",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.3.1",
    "@commitlint/config-conventional": "^20.3.1",
    "husky": "^9.1.7"
  }
}
